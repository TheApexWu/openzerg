// Package pi contains helpers related to the PI agent that runs inside each
// attacker pod. The control plane does not invoke PI directly; this package
// hosts shared types and prompt-template helpers.
package pi
