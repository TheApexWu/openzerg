# Ripped RA2 sprites / voices / soundtrack — DO NOT COMMIT (gray-zone IP).
# Each developer sources their own copy locally for demo machines.
assets/

# Conversation transcript scratch from prep session — not source.
Untitled

# Local briefs / asset bundles used during hackathon prep.
OPENZERG_PIVOT.pdf
openzerg-assets.zip

# Local node / build artifacts (if added later)
node_modules/
dist/
.cache/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
